name: Skyramp Testbot

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.head.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  testbot:
    runs-on: ubuntu-latest
    if: >-
      github.event_name != 'issue_comment' ||
      contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)
    permissions:
      contents: write
      pull-requests: write
    steps:
      # testbot v0.10.x inputs are camelCase. SUT = Gitea built from source on :3001 (token auth).
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: skyramp/testbot@v0.10.4
        with:
          skyrampLicenseFile: ${{ secrets.SKYRAMP_LICENSE_FILE }}
          anthropicApiKey: ${{ secrets.ANTHROPIC_API_KEY }}
          targetSetupCommand: './deployment-testbot/setup.sh'
          targetReadyCheckCommand: 'curl -f http://localhost:3001/api/healthz'
          targetReadyCheckTimeout: 600
          authTokenCommand: './deployment-testbot/get-token.sh'
          autoCommit: true
          postPrComment: true
